
const numbers = [0, 12, 8,  5, 8, 71, 120, 500, 9, 5];

// Agregar elementos a mi arreglo, pero con este método se requiere conocer la longitud del arreglo 
//numbersLeght = numbers.length
//console.log(numbersLeght)
//numbersLeght [numbersLeght] = 100;

//console.log(numbers);

// Con el método push se agregan elementos al final del arreglo
numbers.push(700, 800);

console.log(numbers)

//Método unshift para agregar elementos al iniciar un arreglo
numbers.unshift(-3, -2, -1);

// Método Pop para eliminar el Último Elemento De mi Arreglo.

numbers.pop();
console.table(numbers);

// Método  Shift para eliminar el primer elemento de mi arreglo
//numbers.shift();
//console.table(numbers);

// Método splice elimina los elementos a partir del indice asignado.
numbers.splice(5); 
console.table(numbers);

numbers.splice
