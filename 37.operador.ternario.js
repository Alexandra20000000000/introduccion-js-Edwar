// El operador ternario en JS es una forma corta y elegante de una estructura if ..else

// Se llama ternario porque tiene tres operadores, Observemos la sintaxis
//1. Condición ? valorSIverdadero : ValorSIfalso

//Ejemplo 1

let age = 19;
let message = age >= 18 ? "Eres mayor de Edad" : "Eres menor de edad"
console.log(message);











